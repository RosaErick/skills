# Deliver an application feature

Trace the affected user flow through its existing UI, API and data boundaries. Implement a coherent slice with appropriate validation and error handling. Keep established interfaces unless the requirement changes them.

Run focused checks during development and required project checks before delivery. Review the net change including local files, fix in-scope findings and verify the result. Continue through the requested outcome; scaffolding and a plan alone do not complete a feature request.
