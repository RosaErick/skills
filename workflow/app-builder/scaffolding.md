# Scaffold a project

Inspect the target directory and existing files. For a new app, select the relevant template and compatible supported package versions; for an existing app, reuse its structure. Review a generator's target and options before running it and preserve unrelated files.

Add only the dependencies and configuration needed for the requested slice. Record environment variable names with placeholders and use existing secret handling. Run the actual startup/build command and fix initial integration failures before continuing feature work.
